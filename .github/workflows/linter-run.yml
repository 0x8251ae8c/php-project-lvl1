name: linter-run
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
        # install project
      - name: install
        run: make install
        # run linter
      - name: lint
        run: make lint
